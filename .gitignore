tmp/

# Local runtime artifacts
/signing.key
/media_store/
*.log
/katrix

# Node / web build
web/node_modules/
web/src/routeTree.gen.ts
web/dist/

# Built web assets are regenerated by `pnpm -C web build`; only the committed
# placeholder index.html is tracked so the Go binary always embeds something.
internal/webui/dist/assets/

# ZCode local session/plan artifacts
.zcode/

# Compiled helper binaries
/testreport
sytest-logs-local/

# Local sytest repro harness (sytest checkout + run logs); CI re-checkouts both.
/sytest-src/
/sytest-logs/
