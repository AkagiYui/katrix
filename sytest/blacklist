# SyTest blacklist: tests expected to FAIL.
#
# As of 2026-08-06 (workflow 31098062808) this file is intentionally EMPTY: the
# whitelist now contains every SyTest case, so nothing is masked as "expected
# fail" and every failing test is a real signal (a red sytest step is a genuine
# regression against the 2026-08-06 baseline). Previously this list carried the
# ~208 known-gap tests; they now fail loudly instead of being counted as
# expected fail, so the pass rate and the per-test breakdown in the summary
# reflect reality.
#
# If you ever need to mask a known-unfixable gap again, add its exact test name
# here (and remember to also keep it in the whitelist, since run-tests.pl marks
# any test that is in the blacklist OR missing from the whitelist as expected
# fail). Lines starting with '#' are ignored.
