package federation

import "net/http"

// registerTransactions wires PDU/EDU transaction and room-join federation
// routes. The signature-verified transaction surface is built out in P5.
func (a *API) registerTransactions(mux *http.ServeMux) {}
